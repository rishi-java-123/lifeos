import React from 'react';
import {ArrowRight,LogIn,UserPlus} from 'lucide-react';
import {Input} from '../../components/Common';
export default function AuthPage({mode,onSubmit,message,go}){const register=mode==='register';return <div className="authPage"><div className="authShell"><button className="backLink" onClick={()=>go('landing')}>‹ Back to home</button><button className="authBrand" onClick={()=>go('landing')}><span className="lifeRing"></span><b>Life OS</b></button><div className="authCard"><div className="authIcon">{register?<UserPlus/>:<LogIn/>}</div><h1>{register?'Create your Life OS':'Welcome back'}</h1><p>{register?'Start with the essentials. Personalize it anytime.':'Sign in to continue to your personal workspace.'}</p><form onSubmit={onSubmit}>{register&&<Input name="name" label="Full name"/>}<Input name="email" label="Email address" type="email"/><Input name="password" label="Password" type="password"/>{register&&<Input name="confirm" label="Confirm password" type="password"/>}{message&&<div className="authError">{message}</div>}<button className="primary authSubmit">{register?'Create account':'Sign in'} <ArrowRight size={17}/></button></form><div className="authSwitch">{register?'Already have an account?':'New to Life OS?'} <button onClick={()=>go(register?'signin':'register')}>{register?'Sign in':'Create account'}</button></div><small className="demoNote">Demo authentication is stored only in this browser. Connect a backend before production use.</small></div></div></div>}
